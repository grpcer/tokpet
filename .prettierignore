dist
node_modules
coverage
package-lock.json
# Vendored third-party assets (e.g. minified open-props) — keep verbatim
public/vendor
LICENSE
NOTICE
CODE_OF_CONDUCT.md
# ESP-IDF firmware project — managed by its own (C/CMake) toolchain, not by
# the companion's JS/Prettier formatting.
firmware
